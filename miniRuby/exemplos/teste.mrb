puts 'Escolha dois numeros: ';
min = gets.to_i;
max = gets.to_i;

# Invert them if necessary.
min, max = max, min if min > max;

# Compute the sum.
sum = 0;
puts min..max;
for e in min..max do
  sum = sum + e;
  puts sum;

end
